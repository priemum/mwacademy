<!DOCTYPE html>
<html lang="en">

<%- include("./partials/header.ejs") %>

<body>
     
     <%- include("./partials/navbar.ejs") %>

     <div class="container">
          
          <section class="section-one">
               <div class="fade-section-one">
               </div>
               <div class="section-1-content">
                    <h2>اعادة تعيين كلمة المرور</h2>
               </div>
          </section>
<br>
          <section class="section-two">
               <div>
                    <div class="con-head">
                         <p>Money War Academy Account</p>
                    </div>
                    <form method="POST">
                         <% if(errors) errors.array().forEach(error => { %> <span class="error-msg"><%= error.msg %></span> <% }) %>
                         <% if(note) { %> <span class="note-msg"><%= note %></span> <% } %>
                         <% if(error) { %> <span class="error-msg"><%= error %></span> <% } else { %>
                         <div class="input-con">
                              <span>*كلمة المرور الجديدة</span>
                              <input type="text" name="password" id="password-input" placeholder="••••••••">
                         </div>
                         <div class="input-con-btn">
                              <button type="submit">اعادة التعيين</button>
                         </div>
                         <a href="/login">العودة لتسجيل الدخول</a>
                         <% } %>
                    </form>
               </div>
          </section>
          <br><br>

     </div>

     <br><hr>

     <%- include("./partials/footer.ejs") %>

</body>
</html>
